For any $c > 0$, we have $\DTIME{t(n)} \subseteq \DTIME{ct(n) + n
+ 2}$.

$\DSPACE{s(n)} \subseteq \DSPACE{cs(n)}$

TMs with no work tape decide regular languages.

COPY requires quadratic time on 1-tape TMs.

$\DSPACE{o(loglog(n))} = \DSPACE{O(1)}$

LEN requires logarithmic space.

1-tape Universal TM with t.s time and s space.

Time hierarchy theorem

Space hierarchy theorem

Hennie-Stearns simulation

Translation lemma and finer separations.

Gap theorem

Blum's resource axioms.

Some problems have no fastest algorithm.

$\P$ is invariant.

$\NP$ is invariant.

$\P \subseteq \NP$

Poly-time Turing reductions are reflexive and transitive.

Poly-time many-one reductions are reflexive and transitive.

Poly-time many-one reductions are modifiable for $\NP$.

Reductions that work for quasi-linear time.

$\NP$ is closed under union, intersection, XOR.

Non-deterministic machine based definition for $\NP$.

$\CSAT$ is $\NP$-complete.

$\CSAT$ is self-reducible.

All $\NP$ search problems are solvable if $\P = \NP$.

$\3CNFSAT$ is $\NP$-complete.

Best algorithm for $\CSAT$.

$\NP$ intermediate problems.

Relativization barrier.

$\NTIME{n} \neq \DTIME{n}$

$\NTIME{n^k} = \DTIME{n^k}$ implies $\P \neq \NL$
